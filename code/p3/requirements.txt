# P3 — ML Mühendisi requirements (Modül A v2, Pomzadoya)
# Hedef ortam: Colab Pro (A100/T4) / Kaggle GPU / yerel CUDA 11.8+

# Core deep learning
torch>=2.1.0
torchvision>=0.16.0

# Segmentation backbone library (U-Net + encoders)
segmentation_models_pytorch>=0.3.3

# timm encoders (SSL4EO-S12 backbone uyumu)
timm>=0.9.12

# Geospatial I/O
rasterio>=1.3.9

# Numerics
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3.0

# Grad-CAM (T3.11)
grad-cam>=1.5.0

# HuggingFace (SSL4EO-S12 weights)
huggingface-hub>=0.20.0

# Logging (opsiyonel; WANDB_API_KEY env varsa aktif)
wandb>=0.16.0

# Augmentation (P2 ile entegrasyon)
albumentations>=1.3.1

# Progress
tqdm>=4.66.0

# YAML config
pyyaml>=6.0

# ONNX export (T3.13 opsiyonel)
onnx>=1.15.0
onnxruntime>=1.16.0
