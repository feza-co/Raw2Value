# P4 Spektral Mühendis — Python paket gereksinimleri
# Hedef Python: 3.10+
# Hedef ortam: Colab veya yerel conda env (gdal binary için conda öneriliyor)

# Raster I/O
rasterio>=1.3.9
GDAL>=3.6                  # conda: `conda install -c conda-forge gdal`
numpy>=1.24
scipy>=1.10

# CMR / Earthdata download
requests>=2.31
beautifulsoup4>=4.12       # CMR HTML scraping fallback
lxml>=4.9                  # bs4 hızlı parser

# Yardımcı
matplotlib>=3.7            # histogram / sanity preview
tqdm>=4.65                 # download progress

# Geliştirme (opsiyonel)
pytest>=7.4                # 07_fusion_prototype + fuse_confidence test
